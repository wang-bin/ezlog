#include <ezlog/xxx.h>
log_tag
log level: stderr, stdout
printer: the non-keyword print combind as one to reduce the list size
eztime: ref to QDateTime. time is not correct. msec support
appender: api change to appender(int level, const char* msg)
android log appender implemention
print to debugger in windows like qDebug
static library link: -force_load
check wrong list.h usage
Thread safe check
qdoc

syslog appender
google break-pad
qDebug sql handler

strftime(s, sizeof(s), "%Y-%m-%d %H:%M:%S", localtime(&t));

Object level debug information!!

参考 boost.thread
mutex
layout

