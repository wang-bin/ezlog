#include <ezlog/xxx.h>
log_tag
log level: stderr, stdout
printer: the non-keyword print combind as one to reduce the list size
eztime: ref to QDateTime
android log appender implemention
print to debugger in windows like qDebug
Auto ezlog_init_default
More understandable type and var name: e.g. appender_t
Thread safe check
Copyright date
qdoc

global layout, appender's layout
syslog appender
error message system

qDebug sql handler

ezlogxx
strftime(s, sizeof(s), "%Y-%m-%d %H:%M:%S", localtime(&t));

参考 boost.thread
mutex
layout

